{% extends 'base.html.twig' %}

{% block contenu %}
    <div class="table-responsive containerIndex">
    <a class="btn btn-info btn-index" href="{{ path('salarie')}}">Salariés</a>
	<a class="btn btn-info btn-index" href="{{ path('entreprise')}}">Entreprise</a>
    <table class="table">
        <thead>
            <tr>
                <th>RAISON SOCIALE</th>
                <th>SIRET</th>
                <th>ADRESSE</th>
                <th>CP</th>
                <th>VILLE</th>
                <th>MODIFIER</th>
                <th>SUPPRIMER</th>
            </tr>
        </thead>
        <tbody>
            {% for entreprise in entreprises %}
                <tr>
                    <td><a href="{{ path('entreprise_show', {'id':entreprise.id}) }}">{{ entreprise.raisonSociale }}</a></td>
                    <td>{{ entreprise.siret }}</td>
                    <td>{{ entreprise.adresse}}</td>
                    <td>{{ entreprise.cp }}</td>
                    <td>{{ entreprise.ville }}</td>
                    <td><a class="btn btn-warning" href="{{ path('entreprise_edit', {'id':entreprise.id}) }}">Modifier</a></td>
                    <td><a class="btn btn-danger" href="{{ path('entreprise_delete', {'id':entreprise.id}) }}">Supprimer</a></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    </div>
    <br>
    <a class="btn btn-success" href="{{ path('entreprise_add') }}">Ajouter</a>
{% endblock %}
